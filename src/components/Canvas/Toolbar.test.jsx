import { render, screen } from "@testing-library/react";
import Toolbar from "./Toolbar";
import { describe, expect, test } from "vitest";

describe("Rendering the component as intended", () => {
  test("Rendered with the canvas", () => {});
});
