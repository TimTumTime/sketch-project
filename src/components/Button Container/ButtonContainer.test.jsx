import { test, describe, expect } from "vitest";
import ButtonContainer from "./ButtonContainer";

describe("Rendering the component properly", () => {
  test("Render 5 level components on the structured learning page", () => {});
  test("Render 5 topic components on the structured learning page", () => {});
});

describe("Component functions as intended", () => {
  test("Clicking the level component sends the user to a learning page", () => {});
});
